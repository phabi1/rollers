import { DataloaderInterceptor } from './dataloader.interceptor';

describe('DataloaderInterceptor', () => {
  it('should be defined', () => {
    expect(new DataloaderInterceptor()).toBeDefined();
  });
});
